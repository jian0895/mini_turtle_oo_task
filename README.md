# mini_turtle_oo_task
# Mini Turtle OO 🐢

Un paquete educativo en Python que refactoriza un sistema de dibujo tipo *turtle* en texto (ASCII),
utilizando **Programación Orientada a Objetos**, **encapsulamiento** y **eliminación de variables globales**.

El objetivo principal es demostrar cómo múltiples objetos pueden mantener estados independientes
utilizando atributos de instancia.

---

## 🎯 Objetivo del Proyecto

- Refactorizar un paquete funcional a un diseño orientado a objetos
- Eliminar variables globales
- Aplicar encapsulamiento usando atributos de instancia
- Permitir múltiples tortugas independientes
- Exponer una interfaz clara mediante una clase

---

## 📦 Instalación

Este paquete no está publicado en PyPI. Puede instalarse localmente de la siguiente manera:

```bash
git clone https://github.com/jian0895/mini_turtle_oo.git
cd mini_turtle_oo
pip install .
